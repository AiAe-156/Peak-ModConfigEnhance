# ModConfigEnhance 1.0.10 验证记录

## 本次改动

- 左栏滚轮强度从 40 改为 40/3；独立的 0.1 秒平滑插值，只对挂标记的左栏 ScrollRect 生效。外部位置接管、拖拽、禁用、关闭撤销待完成目标。
- 顶部说明栏在切模组/分类时显示模组名、版本、当前分类、可管理配置项总数，标题稍突出；进入右侧具体配置项后恢复说明。无虚构的模组简介。
- “补漏翻”按钮兼容旧词典中“查漏翻/补翻译/補翻譯”已知旧名称，仅内存迁移，不写回词典。
- 语言应用/重载成功后，说明栏按实际加载顺序显示全部语言文件名，可滚动阅读；保留结果直到出现新的具体配置项 hover/focus。
- README 中英文与运行时 readme 的简中/繁中/英文同步。

## 验证证据

- Release 使用当前项目实际游戏引用编译：0 警告、0 错误。
- SidebarWheelTests 直接编译真实 SidebarWheelScroll.cs：通过。覆盖标记限定、方向、40/3 步幅、边界、0.1 秒抵达、外部接管、关闭、禁用时取消目标。
- LanguageGroups 直接编译真实 Loc.cs：通过。既有分组/合并/失败保持/XUnity 镜像逻辑回归，新增文件清单完整性/顺序与旧按钮名称兼容断言。
- 替身测试不等于 Unity 引擎内或真实 XUnity 联动实测。说明栏状态切换、颜色和实际鼠标/手柄手感仍需进游戏验证。

## 回退备份

- 源码与文档：备份/ModConfigEnhance_1.0.9_20260915_010908。
- 部署前 DLL/README/CHANGELOG/manifest：备份/部署前_1.0.10_20260915_012411。

## 建议游戏内验收

1. 左栏滚动一格，确认距离变小、短缓动无长惯性；滚动中拖动滚动条或切手柄定位不回拉。
2. 切换左侧模组和分类，检查概况名/版本/分类/总数；移入右侧选项检查说明接管。
3. 选语言或点重载，检查分包文件名按加载顺序列全，旧右侧焦点不会立刻盖掉结果；长清单可滚动。
4. 旧 zh-cn.txt 不手改，按钮应显示“补漏翻”；简繁切换和分包加载仍正常。