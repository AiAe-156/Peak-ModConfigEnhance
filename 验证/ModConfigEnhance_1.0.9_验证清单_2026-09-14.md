# ModConfigEnhance 1.0.9 验证记录

## 改动范围

- 同语言码的分包共同加载：基础文件、补翻、个人修正保持独立。
- 简称 zh/en 与大小写兼容；简繁语言组隔离；文件名文本排序及有效译文覆盖。
- 语言下拉按组显示；查漏翻跳过已翻界面词条；组解析失败保持旧译文。
- 左栏从选中模组行向上下同时渐入，底部选中时向上；两侧动画状态隔离，关闭恢复待播行。
- README（中英文）与运行时 readme 模板（简中/繁中/英文）同步更新，版本统一 1.0.9。

## 已验证

- Release 构建：游戏现用程序集引用，0 警告、0 错误（最终改动后复核结果见下）。
- SidebarCascadeTests：直接链接实际 SidebarCascade.cs，测试顶部/中间/底部/无选中逐波序列；首波立即启动、上下同拍、表头不重复；重启仅停止对应协程，关闭恢复已播/待播行。
- 测试使用最小 Unity 替身，不等于 Unity 引擎或游戏内动画验证。

## 游戏内待验证

1. 打开配置页，选中靠下模组，关闭再打开；选中行首波出现，上下邻行同时展开。
2. 最底部选中后重开，应向上依次出现；连续开关或切换分区，不应留下透明空行。
3. 保留现有简中词典，加入 zh-cn_20_补翻.txt 与 zh-cn_90_个人修正.txt，重载后新增译文生效且原译文保留。
4. 同键冲突采用后加载的有效译文；空值及英文占位不覆盖已有译文；切繁体不混入简体。
5. 新增或修改分包后重载；损坏组文件时有文件名/行号提示，旧显示与镜像保持。

## 备份

- 源码及项目元数据：备份/ModConfigEnhance_1.0.8_20260914_011924。
- 部署前 DLL 和说明：备份/部署前_1.0.9_20260914_012846。
## 最终代码验证

- 最终 Release 编译成功：0 警告、0 错误。
- LanguageGroups：直接链接生产 Loc.cs，PASS。覆盖别名/大小写/排序/区域隔离、同文件与跨文件占位保护、英文组有效内容、Apply/跟随模式、XUnity 镜像文件、坏分包保持旧表与旧镜像，以及查漏翻 MCE 识别。
- SidebarCascadeTests：PASS。真实 SidebarCascade.cs 配最小引擎替身，验证实际波次、首波执行、协程停止与透明度恢复。
- 独立审查发现的两项问题已修：备用词典忽略纯空格译文；导出计数包含 MCE 词条。
- 上述是静态、构建及替身测试证据，不代表真实 Unity / XUnity 游戏运行验证。

## 部署结果

确认 PEAK 未运行后按现有 csproj Deploy 目标部署，构建成功；目标目录中的 DLL、README、CHANGELOG、manifest 均存在，manifest 为 1.0.9。未做例行哈希校验。独立审查已复核两项修复及 SidebarCascade，无新增发现。运行时目录说明会在下次启动或切换语言时自动生成新版。
